# Archivo Familiar v4.2.0-alpha3

## EP-004.2 Fase A — Corrección

Las líneas del árbol se dibujaban, pero las tarjetas no llegaban a insertarse
porque `treeNodeLabel()` utilizaba una función inexistente (`escapeHtml`).
Se sustituye por `esc`, la función de escape HTML de la aplicación pública.
